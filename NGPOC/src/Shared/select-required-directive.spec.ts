import { SelectRequiredDirective } from './select-required-directive';

describe('SelectRequiredDirective', () => {
  it('should create an instance', () => {
    expect(new SelectRequiredDirective()).toBeTruthy();
  });
});
